<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8" />
    <style media="print">
      @page 
      {
        size: auto;   /* auto is the initial value */
        margin: 0mm;  /* this affects the margin in the printer settings */
      }
    </style>
    <style>
      body { text-align: center; }
      figure { margin: 0; width: 18%; padding: 1%; text-align: center; border: 1px dotted black; display: inline-block; }
    </style>
  </head>
  <body>
    {% for entity in entities %}
    <figure>
      <img src="http://www.racoindustries.com/barcodegenerator/1d/barcode-image.axd?S=Ean8&amp;AC=T&amp;BH=0.2&amp;BW=0.015&amp;C={{ entity.code }}&amp;DC=T&amp;EUS=None&amp;GB=T&amp;IFMT=Png&amp;QZ=0.25" />
      <figcaption>{{ entity.student }}</figcaption>
    </figure>
    {% endfor %}
  </body>
</html>